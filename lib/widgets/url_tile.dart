// Widget representing a single tracked URL with its status and actions.

import 'package:flutter/material.dart';

class UrlTile extends StatelessWidget {
  const UrlTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(); // TODO: Show URL, status code, check/remove buttons
  }
}
