// Widget displaying a list of all tracked URLs.

import 'package:flutter/material.dart';

class UrlListView extends StatelessWidget {
  const UrlListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(); // TODO: ListView.builder rendering UrlTile widgets
  }
}
