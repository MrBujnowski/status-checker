// Sends notifications to a user's Discord webhook.

class DiscordService {
  Future<void> sendNotification(String webhookUrl, String message) async {
    // TODO: Send message to the specified Discord webhook
  }
}
